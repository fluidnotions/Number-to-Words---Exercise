{
  "name": "numbers-to-words",
  "version": "0.1.0",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/jest": "^27.5.2",
    "@types/node": "^16.18.0",
    "jest": "^26.5.3",
    "ts-jest": "^26.4.1",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "server:build": "rm -rf dist-server && tsc",
    "server:run": "node dist-server/Server.js",
    "server:start": "npm run server:build && npm run client:start"
  }
}
